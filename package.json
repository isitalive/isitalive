{
  "name": "isitalive",
  "version": "0.7.2",
  "license": "AGPL-3.0-only",
  "description": "Lightning-fast dependency health checker — is this open-source project safe to depend on?",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250313.0",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.5.0"
  }
}
